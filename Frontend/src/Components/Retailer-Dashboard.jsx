// Retailer dashboard removed — retailers are no longer supported as a separate dashboard.
// If you need to reinstate it later, restore from git history or re-add the component.

export default null;
